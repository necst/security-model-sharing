services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: survey-plot
    working_dir: /app
    volumes:
      - ..:/app/output
    networks:
      - survey

networks:
  survey:
    driver: bridge
